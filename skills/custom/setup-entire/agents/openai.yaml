interface:
  display_name: Setup Entire
  short_description: Enable Entire in this repo with agent hooks and commit linking
  default_prompt: Use $setup-entire to check this repository's Entire setup and enable
    it for claude-code, codex, pi and gemini.
policy:
  allow_implicit_invocation: false
  products:
  - chatgpt
  - codex
  - api
  - atlas
